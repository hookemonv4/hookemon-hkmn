# Separate local seed receipts

The isolated pre-seed fixture passed export once and then executed exactly three successful local transactions: USDG approve(Permit2,100000000), Permit2 approve(USDG,hook,100000000,max48), and the production hook seedCanonicalLiquidity call by its configured local test authority. Gas is respectively 57,916, 47,602 and 664,385, total 769,903 EVM gas. No swaps or claims were repeated. Receipts and exact calldata are attached.

Candidate parameters remain 100 USDG, the full 1e27 HKMN launch allocation, ticks [-887220,887220], liquidity 316227766016837933 and sqrtPriceX96 250541448375047931344782464160941919862. The exported seed calldata binds these parameters, payer, custody and deadline. It uses the previously documented owner caps in the copied test harness, local test authority 0xA11CE and payer 0xBEEF. No live approval or replacement of the historical 240-USDG seed is asserted.

Payer USDG fell from 100000000 atomic to zero. Aggregate PoolManager USDG increased from 52925837235188 to 52925937235188, exactly 100000000 atomic. Hook HKMN decreased from 1e27 to zero and PoolManager HKMN increased from zero to 1e27. Position 2120364 is owned by custody 0x9D6895a38e5D58C931837e74a41fD98E0daC32F8, verified via PositionManager ownerOf after the seed receipt. Conservation uses manager deltas, not its unrelated aggregate balances.

Public Robinhood block 57010372 supplies the fork state. Anvil is isolated on loopback 18550, session 20236; previous studies were untouched. Hook, token and custody runtime templates match the tracked launch artifacts outside compiler-defined immutable references. Runtime lengths remain 30033, 3201 and 3040 bytes. Local virtual ETH funds only test transaction execution. The copied exporter stops before approvals and seed; production source is unchanged.

Reproduce by placing the archived source copies at their original test paths, using the archived package foundry.toml with FOUNDRY_PROFILE=launch and the repository FOUNDRY_ALLOW_PATHS, then run forge test --root packages/contracts --match-contract LocalGasSeedExportTest --match-test testExportExistingFixtureOnly -vv against the pinned public fork. Initialize a fresh Anvil with the resulting alloc at that block, then run measureseed.py from the worktree. Archive access may be needed later. This is conditional local EVM gas, excluding deployment, provider fees and Arbitrum poster/L1 charges; it is not a live maximum or the full 250-USD budget proof.

The raw export metadata retains the inherited initialTraderUsdgAtomic label from the 150-USDG fixture template; this seed-only export does not fund that trader. Its authoritative starting inventory is the measured payer and hook balances in summary.json, totaling 100 USDG plus the existing HKMN allocation.
