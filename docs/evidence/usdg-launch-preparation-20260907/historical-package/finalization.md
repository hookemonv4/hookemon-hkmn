# USDG launch finalization

The owner retained USDG as the quote asset. This preparation does not authorize funding, signatures, deployment or broadcast. The complete current V2 context is available in `evidence/provider-capabilities-resume.json`; request-specific USDG admission is still unproven. Public readiness alone does not complete source or economic admission.

## Complete the deterministic request

1. Obtain the supported USDG request construction route and the provider's source-lineage/origin commitment convention. Keep the current source and 300/250/40/10-bps economics. The unchanged public packer does not expose USDG selection, and the inventory model is a different implementation. Do not add an undocumented field or patch the official packer.
2. Bind the selected source revision and its complete compiler input, image and evidence files. Retain the existing compiler version/settings and bytecode CI receipts. Finalize actual evidence claims, then derive the complete bundle hash and source descriptor. Caller attestation records real checks and timestamps, never provider approval.
3. Confirm the seed and process-claim count. The 250 USD ceiling is fixed; the historical 240 USDG seed and local 100+50 USDG scenario remain separate. Price every cost before claiming a feasible total. Keep `processClaimMaxCount` null until the recorded suggestion is affirmatively selected.
4. Use `constructor-bindings.json` for ABI fields. Determine the project's nonzero binding/runtime commitment preimages. Derive the token with its immutable price; verify the resulting address is consistent with the currency ordering assumed by that price. Recompute through the documented producer if it is not. Derive custody and hook using the exact factory salt rules, source locators and constructor bytecode. Verify hook address low 14 bits equal 0x20cc.
5. Materialize all compiler immutable references from exact constructor execution/semantics and compare the resulting runtimes with independent simulation. A runtime template hash does not identify a deployed target. Preserve all constructor arguments, effective salts, init-code hashes, predicted addresses and derivation inputs.
6. Encode the three factory calls in order: token `allocate(hook)`, custody `configureBindingHook(hook)`, hook `initializeGraphLaunch(custody,sqrtPriceX96)`. Bind the same USDG/HKMN PoolKey, fee zero and spacing 60 in both the hook and provider route. The custody constructor tokenId is zero; it receives the minted position later.
7. Finalize funding/liquidity declarations for separate owner seed, zero initial native buy and zero graph call value. Obtain a real gas bound. Create a fresh request nonce/window only when all inputs exist, run the unmodified supported producer and save exact canonical bytes. Use documented non-reserving preflight. A source result of `evidence_required` is not admission or signability.

## Prepare each wallet step

The launch wallet is `0xfc82B0da6d487B97d7eA1AA0d51E00AfF4F3a729` on chain 4663. For each transaction show the exact recipient, decoded function/arguments, calldata hash, native value, token movement, spender/allowance, gas estimate and maximum cost. Use pending wallet nonce and current chain/provider state at final review; do not store a long-lived signing window now.

The graph transaction must match the admitted request and expected runtime hashes. After its authorized execution, read back token name/symbol/decimals/supply, allocation, graph roles, custody binding and canonical PoolKey before constructing the seed approval sequence. A pre-execution simulation may preview those checks, but only receipts and code reads establish deployment.

Read the launch wallet's USDG balance and both allowance layers. Approve the exact selected USDG seed to Permit2, then the same exact amount to the derived hook with a fresh expiration. Reconcile an existing allowance before presenting new approvals. Decode the seed tuple against actual token ordering, exact liquidity and amount maxima; payer is the launch wallet and custody is the derived permanent custody. Apply the recorded seed deadline ceiling of 900 seconds after wallet confirmation.

After authorized seeding, verify the position NFT owner is permanent custody, tokenId is bound once, ticks/liquidity/PoolKey match, the hook has no residual HKMN, unused USDG returned to the payer and allowance readbacks match the seed behavior. These are future execution checks, not current claims.

## Complete the operating cycle

The original task owns bot integration, live Collector binding and serial main merges. Require its current acceptance before calling the complete product ready. Exact trading, process-claim, bridge, card purchase/sale, return and holder-payout transactions depend on those bindings and current quotes. Local gas receipts and virtual capital conservation remain simulation evidence. Do not count recycled process-claim capital twice or assume USDG/USD parity.

No automatic execution follows this document. The final wallet packet is presented for the owner's separate review and signatures.
