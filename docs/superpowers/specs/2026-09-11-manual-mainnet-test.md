# Manual mainnet test scope

This proposal records the owner's explicit 2026-09-11 request. It does not approve missing release anchors or alter the authoritative Hookemon launch economics.

The requested test starts once from the authenticated dashboard, uses existing Operations wallet funds without a hook claim, bridges to Solana, buys and opens one 25-dollar pack, performs the existing buyback, bridges attributable proceeds back and allocates them proportionally to the top 100 eligible Programmable V4 holders. The owner selected token 0xC60bA256B44334A0Cd2C7242E98B88f031abB006 through the supplied DEX Screener pool. The recipient list precedes execution; real website results follow execution. A later display reset preserves immutable payment evidence.

The implemented preparation adds the private manual request API, one-request persistence, same-cycle recovery and dashboard controls. It preserves production refusals until a separately reviewed direct-wallet funding stage and external-token snapshot binding exist. It does not relabel HKMN snapshots or process claims as the requested test. Authentic Collector release anchors, native payment authority and finalized recipient evidence remain prerequisites for live admission.

The rationale for a separate manual profile is that an ordinary scheduler tick may start additional cycles and the existing production pipeline draws from Hookemon process claims. Neither behavior matches the owner's bounded existing-wallet test.
