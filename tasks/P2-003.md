# P2-003 — Freeze manual pack selection

## TASK

Import one exact pack snapshot, select one available pack manually, permit bounded draft edits, and freeze one immutable cycle plan.

## RELEVANT FILES

- `packages/runner/src/operator/pack-selection.mjs` — Closed content-addressed snapshot and exact pack selection.
- `packages/runner/src/operator/cycle-plan.mjs` — Draft validation, revision, freeze, and binding validation.
- `packages/runner/src/cycle/bindings.mjs` — Selected-pack and custody binding validation.
- `packages/runner/src/cycle/collector.mjs` — Selected-pack propagation through fixture requests and responses.
- `packages/runner/src/cycle/preflight.mjs` — Frozen pack and distinct return-account preflight.
- `packages/runner/src/cycle/reducer.mjs` — Selected-pack continuity across cycle transitions.
- `packages/runner/src/cycle/schemas.mjs` — Closed cycle artifact schemas.
- `packages/runner/test/operator/pack-selection.test.mjs` — Snapshot and selection vectors.
- `packages/runner/test/operator/cycle-plan.test.mjs` — Draft, freeze, digest, and binding vectors.
- `packages/runner/test/cycle/` — End-to-end selected-pack and custody substitution tests.
- `docs/modules/cycle-runner.md` — Pack and plan contract card.

## PATTERN TO FOLLOW

Follow `docs/modules/cycle-runner.md`, the canonical JSON and SHA-256 helpers, and the exact fixture-binding patterns in the Phase 1 runner. Revalidate the complete object at each boundary.

## CONSTRAINT

Quantity is exactly one and turbo mode is false. Only named draft fields may change before freeze. Pack snapshots, cycle identifiers, nonces, custody identities, route digests, amounts, minimum returns, gas caps, and plan digest are immutable after freeze.

## DEPENDENCIES

- `P2-006` Freeze manual cycle architecture.

## REQUIREMENTS

- `REQ-cycle-control-1` at requirements revision 57.

## ACCEPTANCE CRITERIA

- Canonical sorted snapshots reject duplicate, unknown, changed, decorated, or digest-mismatched pack data, and selection accepts only a pack present in the exact snapshot.
- Draft edits remain bounded and reversible before freeze; canonical freeze binds every execution field and rejects every post-freeze edit or cross-snapshot substitution.
- The selected pack and distinct cycle escrow propagate through every runner request, response, reducer transition, authorization, and preflight without reintroducing a hardcoded pack.

## VERIFICATION

```sh
node --test packages/runner/test/operator/pack-selection.test.mjs packages/runner/test/operator/cycle-plan.test.mjs
node --test packages/runner/test/cycle/*.test.mjs
```

## CURRENT TODOS

- `P2-001` Bind lean manual cycle control.
- `P2-002` Isolate peg cycle custody.
- `P2-003` Freeze manual pack selection.
- `P2-004` Persist local operator control.
- `P2-005` Verify lean Phase 2 boundary.
- `P2-006` Freeze manual cycle architecture.
- `P2-007` Model cycle control feasibility.
- `P2-008` Bind distribution to cycle escrow.
- `P2-009` Preserve cycle progress with quarantined surplus.
- `P2-010` Anchor frozen cycle execution.
- `P2-011` Document Phase 2 task ledger.

## RECENT FAILURES

- A syntactically valid pack is insufficient when it is absent from the exact frozen snapshot.
- Treating the coordinator as the return account defeats per-cycle custody isolation.
