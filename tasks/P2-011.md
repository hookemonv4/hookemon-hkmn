# P2-011 — Document Phase 2 task ledger

## TASK

Provide one self-contained context card for every Phase 2 ledger task and bind the complete card set to requirements revision 57 and architecture revision 5.

## RELEVANT FILES

- `tasks/P2-001.md` through `tasks/P2-011.md` — Self-contained Phase 2 task context cards.
- `tasks/context-packs.json` — Content-addressed task-card registry, dependency map, and revision binding.
- `tasks.json` — Read-only projected ledger truth used to verify titles, risks, requirements, and dependencies.
- `specs/requirements.json` — Read-only requirements revision and identifier source.
- `architecture/capability-map.json` — Read-only product-phase and architecture-revision source.

## PATTERN TO FOLLOW

Follow the P1 task-card section order and the v4 task-context-pack contract. Derive all ledger fields from `tasks.json`, hash exact card bytes, and keep relevant P1 card entries as historical context.

## CONSTRAINT

Do not edit generated `STATE.md`, `state.json`, `tasks.json`, append-only receipts, code, architecture, module cards, or product documents. Cards contain at most three acceptance bullets and name exact focused verification commands.

## DEPENDENCIES

- `P2-001` Bind lean manual cycle control.

## REQUIREMENTS

- `REQ-cycle-control-1` at requirements revision 57.

## ACCEPTANCE CRITERIA

- Every `P2-001` through `P2-011` card contains task, relevant files, pattern, constraint, dependencies, requirement binding, no more than three acceptance bullets, verification, current task context, and recent failure guidance.
- `tasks/context-packs.json` declares product phase 2, requirements revision 57, architecture revision 5, and matches every Phase 2 ledger title, risk, dependency, requirement, path, and exact SHA-256 digest.
- Existing relevant P1 card records remain intact and all registered card paths are regular repository files accepted by the task, trace, and delivery-boundary validators.

## VERIFICATION

```sh
node scripts/v4.mjs trace check
node scripts/check-delivery-boundary.mjs
node --test scripts/tests/delivery-boundary.test.mjs scripts/tests/gates.test.mjs
```

## CURRENT TODOS

- `P2-001` Bind lean manual cycle control.
- `P2-002` Isolate peg cycle custody.
- `P2-003` Freeze manual pack selection.
- `P2-004` Persist local operator control.
- `P2-005` Verify lean Phase 2 boundary.
- `P2-006` Freeze manual cycle architecture.
- `P2-007` Model cycle control feasibility.
- `P2-008` Bind distribution to cycle escrow.
- `P2-009` Preserve cycle progress with quarantined surplus.
- `P2-010` Anchor frozen cycle execution.
- `P2-011` Document Phase 2 task ledger.

## RECENT FAILURES

- A context-pack registry with stale card hashes cannot prove which instructions an implementation consumed.
- Omitting newly split hardening tasks makes the delivery registry and build closure disagree with the ledger.
