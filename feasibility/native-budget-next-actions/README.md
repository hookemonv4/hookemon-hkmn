# Native test-capital candidate

The refreshed conditional subtotal is **$161.090572**, leaving **$88.909428 of unmeasured allowance** against the owner's $250 total-capital envelope. This is not an all-in estimate, a funding approval, or an executable launch package. The previous $161.735674 figure reproduces exactly using its older valuation and gas inputs. Neither figure proves that $250 suffices.

The 12:14 UTC Relay and Robinhood responses remain byte-for-byte unchanged. New public Solana observations at 12:52 UTC measure fees for an unsigned message compiled from that retained return plan. No provider transaction was created, signed, submitted, or broadcast. No buyback income is credited. The return quote assumes a hypothetical 25 USDC input; it is not a forecast of a card's proceeds.

`python3 feasibility/native-budget-next-actions/calculate.py` verifies retained response hashes and reproduces `conditional-calculation.json` with integer ceiling arithmetic. The refreshed ETH mark uses the outbound quote's 10,256,741,228,105,036 wei and $25.341638, not a 1:1 USDC/USD assumption. The mark and 247,272,000 wei/gas snapshot are historical observations, not current execution authority or price caps.

| Counted component | MicroUSD, rounded up | Basis |
| --- | ---: | --- |
| Initial 0.06 ETH capital | 148243799 | Includes 0.04 ETH seed and recycled 0.02 ETH trading float |
| Combined measured execution gas | 10340067 | 16,924,783 gas at retained network price |
| 32 swap intrinsic floor | 410554 | 672,000 gas; calldata and transaction-specific differences remain unknown |
| Seed and claim intrinsic floor | 25660 | Two 21,000-gas floors absent from local `gasleft()` measurements |
| Outbound quoted maximum-fee product | 28412 | 32,971 gas × 348,773,600 wei/gas |
| Entire existing SOL capital footprint | 2042080 | 19,920,066 lamports, marked with retained provider SOL/USD ratio |

The 16,924,783 gas row replaces the old deployment/wiring measurement with the archive route: 10,298,741 + 6,626,042 remaining local seed/swaps/claim gas. The archive route already includes 679,292 intrinsic/calldata gas; do not add that again. Its documented 1,000,000-gas margin is an envelope allowance, not measured consumption. The local funding test measures calls with `gasleft()` in `packages/contracts/test/native/NativeFundingFeasibility.t.sol`; the added floors are not transaction upper bounds. Exact future-address bytecode, calldata and approvals remain unmeasured.

The model's 0.415128299471828390 ETH turnover recycles the trading float. Fee diversion and outbound pack principal already consume the initial capital; adding either again would double count. Outbound provider `fees.gas` is 7,673,019,200,000 wei, whereas the selected maximum-fee product is 11,499,414,365,600 wei: these are alternatives, not additive fees. Relayer gas and service are subcomponents of the relayer total, already reflected in quoted principal.

The fresh quoted first claim would be **10,256,741,228,105,036 wei**. Measured generated process liability is **10,378,207,486,795,709 wei**, leaving **121,466,258,690,673 wei**. The historical claim was only **10,243,579,001,330,370 wei**, short by **13,162,226,774,666 wei** against the changed quote. This is a mismatch between a historical claim and a later quote, not proof that the scenario cannot fund it. The closest verified candidate is to size the first claim from a fresh quote within measured liability and the proposed, unapproved 0.015 ETH limit. No second claim or six-hour replenishment is assumed. Exact fractions and headroom are in `freshOutboundFit`; the future quote and approved limit must be checked before any first claim.

The finalized Solana fee query returns **5,000 lamports** for the actual captured one-signature message at slot 445342887. It contains no ComputeBudget instruction. This proves the message fee, not successful execution, priority inclusion or future fees. The missing source USDC token account is `G4LdLyzDfq6DGt5Ha5C11WydKa2oJ5uNmkYyhLdsLBHo`; the RPC reports **1,855,569 lamports** rent for 165 bytes. The other queried instruction accounts exist. Actual account creation still depends on the chosen path. The retained provider estimate is **304,691 lamports**, an alternative estimate rather than another charge on top of 5,000. Provider estimate plus one token-account rent is **2,160,260 lamports**, within the observed **19,920,066**, leaving **17,759,806 lamports** before other Solana actions. These costs consume the already counted SOL footprint and add zero duplicate dollars to the subtotal.

Recent writable-account priority samples are preserved as observations only. They do not select a future ComputeBudget price or limit. `measure-solana.mjs` uses pinned `@solana/web3.js` 1.98.4 to compile the message without signing; it requires the workspace adapter dependencies. Do not rerun collectors into this retained evidence directory: a fresh observation belongs in a new directory. The exact outbound `eth_estimateGas` request was attempted read-only and returned `-32000: insufficient funds for transfer`; the observed provider gas fields remain the available candidate estimate.

| OPEN FACT | Concrete resolution | Closest verified alternative |
| --- | --- | --- |
| Source-chain acquisition, bridge/spread and original SOL purchase basis | Owner supplies actual funding asset, chain and cost basis; then obtain public exact-route quotes and source fee estimates before any transfer | Retain market-marked 0.06 ETH plus whole observed SOL footprint; reserve the unmeasured allowance, without declaring it sufficient |
| Approved future deployment preimage and transaction gas | After the owner selects the pending address/preimage proposal, derive exact calldata and measure that candidate on the pinned isolated fork; root owns this work | The recorded synthetic archive route plus local funding-call inventory, clearly excluding candidate-specific proof |
| First claim amount for a future quote | Refresh the exact-output quote immediately before the first claim; verify principal against generated liability and the approved claim limit, then measure the bounded scenario | The retained quote fits measured generated liability and the proposed 0.015 ETH cap; the old smaller claim cannot be reused as evidence |
| Outbound actual funded execution gas | Estimate the exact fresh provider transaction after funding is independently authorized and observable; retain raw RPC response | Captured max-fee product above; current estimate attempt explicitly failed for insufficient funds |
| Collector purchase/open/buyback transaction fees, priority settings and account creation | Obtain transaction plans only within separately authorized provider creation; run unsigned fee/rent checks on exact plans and count fee payers/accounts | Actual return-message fee, one missing token-account rent and complete existing SOL footprint; no guessed pack/open/buyback charges |
| Payout recipient count, contracts/rejections/retries and transaction gas | Freeze the real eligibility snapshot and payout plan; estimate exact recipient calls and gas reserves | No hypothetical recipient count or payout expense is assigned; the remaining allowance is not an upper bound |
| Card outcome and actual return proceeds | Observe attributable finalized buyback proceeds, then quote exact return input | Zero income credit; the retained 25-USDC return is an explicitly separate scenario |

RPC semantics are documented by Solana: [message fees](https://solana.com/docs/rpc/http/getfeeformessage), [rent minimum](https://solana.com/docs/rpc/http/getminimumbalanceforrentexemption), [writable-account priority samples](https://solana.com/docs/rpc/http/getrecentprioritizationfees), and [fee structure](https://solana.com/docs/core/fees). Per-observation endpoints, timestamps and hashes are in `observations.json`, `solana-measurement-sources.json` and `outbound-estimate-source.json`. The upstream capital calculation and archive gas inventory hashes are included in `conditional-calculation.json`.
