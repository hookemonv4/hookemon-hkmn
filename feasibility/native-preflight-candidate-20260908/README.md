# Proposed native preflight candidate

This is an **unapproved, read-only scenario**, with 0.04 ETH maximum seed, 0.02 ETH reusable trading float, a 0.015 ETH six-hour claim ceiling and a 0.02 ETH maximum ceiling. Neither ceiling is an owner decision. No API preflight, upload, signature, deployment, funding or active-input edit was performed. The request remains incomplete and must not be POSTed.

The exact full-stock seed helper derives `sqrtPriceX96 = 12527072418752396559322253362376889`, consumes 39,999,999,999,999,657 wei and the complete 1,000,000,000 HKMN stock, and refunds 343 wei from the seed maximum. These are the same 0.04 ETH seed assumptions used by the retained real native funding fixture. The 32-swap, 0.02 ETH float scenario earns 10,378,207,486,795,709 wei of process fees; cumulative turnover is reused capital. The proposed ceilings cover the retained 10,214,270,236,945,073 wei outbound quote principal, whose earlier captured quote is evidence only and supplies no current execution authority.

`materialize.mjs` uses the checksum-verified official CLI 4.1.0 modules and exact retained source-bundle bytes. The official CREATE2 domain framing and effective-salt formula yield proposed token `0x357D13937522240562cd46FD9ec1445aFe4408af` and custody `0xF84900b25512AA7EBbFd6280afF53F099DA15E07`. These predictions do not assert deployed code. The source-bundle origin, proposed public nonce and fixed token/custody salts are retained from the earlier preparation; publication of that exact origin remains unverified. The permit window is captured historical data and must be refreshed before a future request.

The token constructor, both token/custody runtime materializations and all three initializer templates are now concrete. Compiler AST identities bind each runtime immutable. Initializer address words remain zero **only where the official graph format declares address locators**; the graph predictor must later patch the corresponding target identity. This is the official submitted-graph representation, not an invented zero recipient. The hook constructor field inventory supplies the derived token address and proposed claim ceilings separately from unresolved authority digests.

JSON Schema validation reduces the earlier 14 errors to seven: hook applicant salt, hook constructor bytes, hook runtime hash, hook verification constructor bytes, hook runtime materialization, launch-intent hash and agent attestation. It is a structural check, not a full graph or provider acceptance result. No successful attestation is fabricated.

**OPEN FACT — native authority digest preimages.** `scripts/launch/derive-addresses.mjs` validates supplied nonzero `bindingDigest` and `runtimeDigest`; it does not derive their native preimages. The earlier materialization dependency map overstated the availability of that next computation. `HookemonIssuance` retains those digests as runtime immutables, so both creation and runtime bytes depend on them. The historical `RobinhoodBindings.digest` binds a legacy asset model and the hook address/salt/init-code hash; inserting it unchanged into this constructor would create a circular dependency. The native binding owner must identify the existing intended manifest/runtime-authority producer and exact noncircular preimages, or propose their explicit specification. The closest verified result is the token/custody derivation and exact unresolved hook field inventory here. Only hook completion, graph hashes and the dependent POST wait on that fact; no test sentinel digest is substituted.

**OPEN FACT — model admission.** The distributed 4.1.0 packer rejects this custom kernel with `ROBINHOOD_NATIVE_FEE_KERNEL_REQUIRED` before packing. Pure module calculations here preserve the exact 250/40/10 bp model and do not bypass or claim acceptance by that packer. Once the request is genuinely complete and reviewed, an authorized side-effect-free backend preflight can establish the exact model response. A supported composition or exception must be documented if required; substituting the default kernel changes the economics.

`gas-inventory.json` incorporates the green archive-fork route measurement from run 34220664894, job 102042800504. Its 10,298,741 route gas replaces the funding fixture's 7,993,080 local token/hook/custody deployment and graph-wiring gas. The resulting measured scenario subtotal is 16,924,783 gas, before unmeasured costs and separate transaction intrinsic gas. The 1,000,000 gas route margin is reserve headroom, not paid gas. The archive route is measured against its own fixture, not these newly proposed addresses or ceilings. No USD 250 all-in pass follows: acquisition costs, transaction-specific gas, Solana execution costs, actual card proceeds and current valuation remain unmeasured. LP principal, reusable float, fee liabilities and already-spent bridge principal must not be counted as additional fresh capital.

Reproduce the candidate without network access:

```sh
node feasibility/native-preflight-candidate-20260908/materialize.mjs /path/to/checksum-verified/programmable-launch-4.1.0/package
```

The CLI tarball URL and checksum are pinned in `../native-preflight-materialization/sources.json`. The script refuses changed official modules or source-bundle bytes. `validation.json` records the actual seven remaining errors. `sources.json` binds local input files and the candidate outputs; the source revision is retained evidence, not an owner approval.
