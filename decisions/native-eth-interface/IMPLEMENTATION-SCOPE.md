# Native implementation scope

The current owner instruction explicitly selects native ETH for the active Robinhood pool, fees, Operations, return and holder payouts, while retaining Collector Solana USDC, holder rules, permanent liquidity custody and the existing cumulative 250/40/10 basis-point streams. The supplied technical plan defines the native seed, payment-proof, USD valuation and historical-state refusal boundaries. This is the authority for implementing that selected conversion; broad workflow autonomy supplies no additional financial policy decision.

Revision 71 applies the reviewed 22-requirement conversion to PR39 revision 70, with SHA-256 `750a12abf47a771d1181dd6a1782c1b6fbcc4eebb4632e5d1927bf52f4193425`. Independent review found two inherited or proposed divergences: per-swap fee wording conflicted with the existing three cumulative streams, and insured-value substitution expanded held controls. The corrected revision preserves existing cumulative accounting and purchase-cost attribution. The reviewer verified both corrections at `31b7935514ebf85da805677128e2555eecc7fef5` with no remaining finding in that bounded review.

The working integration includes reviewed PR39 revision 70 and current main through `add0d62ca1c1758f367987c832f8e11704ace0c3`. PR39 must still finish current required CI and merge before the coherent native release merges. Component work may proceed against this present baseline and the specified interfaces; final shared schema versions and consumers are reconciled by the coordinator. Existing feasibility freezes and release approvals remain historical until their actual evidence is regenerated.

This record authorizes no exact launch valuation, seed amount, constructor ceiling, provider fee exception, release hash, live Collector anchor, signature, broadcast, deployment or spend. Those facts and any required concrete owner decisions remain separate. It is not an owner gate-approval artifact and must never be passed as one. The native migration ships only as a consistent integration after current CI and independent changed-money review.
