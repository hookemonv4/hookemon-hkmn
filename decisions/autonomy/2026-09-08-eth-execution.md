# Scoped autonomy for the ETH migration

The owner requested continuous, goal-directed Hookemon work and explicitly authorized adjusting global instructions, project instructions and the installed framework, including removing unnecessary merge confirmations. This document records that request; it does not approve an implementation, test result or future financial release hash.

The owner's current words were:

> Wir können sehr gerne alles verändern, die globalen MD-Dateien, wir können die Dateien im Projekt verändern, das Framework, was wir installiert haben, können wir gerne verändern. Wir können auch unnötige Freigaben beim Merge, alles. Wir können alles verändern. Hauptsache, es funktioniert möglichst gut.

The owner also requested that independent ready tasks run through subagents without waiting for the current task. The established objective is the minimum necessary USDG-to-native-ETH migration on Robinhood, completion of the related bot and PR work, a reviewed mainnet test package, and eventual launch after the separately authorized test. Collector's Solana payment asset stays unchanged.

Within that objective, continue authorized implementation, configuration and instruction work, targeted verification, independent review of changed money paths, and repository preparation without repeated routine confirmation. Use scoped task branches and draft pull requests. The owner's prior requests to handle and merge the relevant Hookemon PRs, together with the current request to remove unnecessary merge confirmations, permit serial integration of in-scope PRs after current main is integrated, required CI is green for the candidate, semantic conflicts are resolved and the applicable independent review is complete. This includes the instruction alignment required for this workflow. It is not authority to merge an unrelated PR or a failed candidate.

Ready independent work means work necessary for this objective or separately requested by the owner. It does not include speculative features. Maintain one writer per shared file and one integration coordinator, obey current leases and runtime resources, and dispatch the next ready task at completion events. Preserve unrelated changes, historical evidence and outstanding liabilities.

Existing approvals for the named Programmable and Collector API operations remain valid within their documented scope and limits; they are not expanded here. The latest request does not authorize a new paid service, private-key export, signature, broadcast, deployment, onchain transaction, public announcement or financial-policy change. The explicit exclusion of signing and broadcast remains in force. The 250 USD test-capital ceiling and need for an exact reviewed transaction package remain unchanged.

Authoritative financial requirement changes still need the existing applicable owner decision; do not reinterpret broad workflow autonomy as approval of unseen release hashes, arbitrary gate overrides, NOT_APPLICABLE decisions or accepted financial risk. Platform restrictions and higher-priority instructions remain binding.

This scope lasts for the stated work until the owner changes or revokes it. The owner's concrete instructions take precedence over this summary if there is a discrepancy. Record progress through the supported task and receipt commands, not by editing generated projections or inventing approval evidence.

This wording supersedes the source version bound by r-00648, with no change to authorization. The earlier receipt remains historical and becomes stale because its source hash changed; the replacement receipt binds this version.
