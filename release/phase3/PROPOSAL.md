# Native Hookemon assisted-launch proposal

Revision 0.1.2 supersedes submission `45ddd161-e5bb-4cb0-ad92-8f4eb044d82e` (0.1.1) in the same Hookemon / HKMN project lineage. Historical submissions and their hashes remain unchanged. The current approved configuration is recorded in `decisions/assisted-launch-v012/README.md` and requirements revision 76.

The chain-4663 graph retains `src/HookemonHook.sol:HookemonHook`, `src/launch/HKMNToken.sol:HKMNToken` and `src/bindings/RobinhoodBindings.sol:PermanentPositionCustody`. Select the token by its full source path and contract name because the compiler closure contains another HKMNToken. Preserve the actual constructor ABIs and hook permission mask `0x20cc`.

Required calls execute in order: `token.allocate(hook)` by the token's immutable issuanceAuthority; `custody.configureBindingHook(hook)` by the custody deployer; `hook.initializeGraphLaunch(custody, sqrtPriceX96)` by graphInitializer; then `hook.seedCanonicalLiquidity(params)` by launchAuthority with zero ETH. Deployment/initialization and inventory transactions may be separate. The assisted route must honor those caller roles without replacing the customer's authority wallet to force a single transaction.

Initial inventory remains buyer-funded. The token has 1,000,000,000 HKMN, 18 decimals and atomic supply `1000000000000000000000000000`. Creator ETH contributed to initial liquidity is exactly zero; there is no new creator token allocation. Native ETH is currency0 and HKMN currency1. The pool has zero LP fee, tick spacing 60, lower tick 133500, upper tick 161220, initial sqrtPriceX96 `250929875796514805540091219040452` and position liquidity `421035394154913054639875`. Permanent custody retains the liquidity position and 115 atomic HKMN of rounding dust.

Buys and sells include one 300-bps fee: 20 Programmable, 30 Treasury and 250 process. Programmable receives `0xD88539d3c4C460136a733A3Fd60cf6BF269079da`; Treasury receives `0xfc82B0da6d487B97d7eA1AA0d51E00AfF4F3a729`. Independent cumulative remainder accounting, native backing, exact-input/exact-output handling and authorized claims are preserved. There is no added platform surcharge or 1% buyback fee.

The initial six-hour native claim cap is `9960873688152935270` wei and maximum cap `19921747376305870540` wei. processClaimMaxCount is confirmed as 24 per rolling six hours. Existing role restrictions and cap-change delays remain; operations rotation delay is 43200 seconds. The separate bot-side rolling six-hour USD budget is $25,000, adjustable up to $50,000. No onchain USD oracle is requested. The unchanged curve's $2,509.82 ETH/USD snapshot is a dated calculation reference, not a peg.

Source and compiler artifacts are prepared for platform integration; final runtime commitments, CREATE2 bindings, route namespaces, permits and transaction payloads remain platform supplied. Launch time, optional initial purchase, live deadlines and wallet nonces remain unset. See `launch-plan.md` for configuration and caller mapping. No deployment, transfer, live launch request or signature is authorized by this preparation.
