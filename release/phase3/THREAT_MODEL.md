# Native assisted-launch threat model

An incorrect source closure, compiler setting, constructor, runtime commitment or CREATE2 binding can produce a different deployment from the reviewed candidate. Verify the three fully qualified production targets, actual eighteen-field hook constructor tuple and permission mask `0x20cc`. Compiler-emitted immutable references belong to the newly compiled source; old runtime hashes or relabeled immutable IDs cannot establish the new artifacts' identity. Standard JSON is compiler input, not a production API launch request.

Preserve token allocation, custody configuration, graph initialization and inventory seeding in that order, with their existing issuanceAuthority, custody deployer, graphInitializer and launchAuthority callers. Separate transactions may implement the assisted route. Changing the customer authority wallet to make an atomic sequence possible would change authorization and is outside the approved configuration.

The inventory seed has zero creator ETH and `msg.value == amount0Max == 0`. Validate spacing 60, ticks 133500 and 161220, initial sqrtPriceX96 `250929875796514805540091219040452`, liquidity `421035394154913054639875` and permanent custody of the position plus 115 atomic HKMN of rounding dust. Native ETH has no token allowance. Custody cannot withdraw, approve or delegate the position. Preserve seed authorization, one-time initialization and rejection of invalid parameters.

Fee accounting preserves cumulative 20/30/250-bps liabilities on gross native quote volume, exact-input/exact-output behavior, partial-fill refusal, fee backing and same-pool call restrictions. The inclusive 300-bps total applies to buys and sells without an added platform surcharge or 1% buyback fee. The immutable platform beneficiary is `0xD88539d3c4C460136a733A3Fd60cf6BF269079da`.

Actual buyer ETH must settle before the hook collects its fee. The routing fixture uses explicit native SETTLE, SWAP_EXACT_IN_SINGLE, then TAKE_ALL for HKMN. A mock router or local pinned PoolManager result does not prove the deployed UniversalRouter's exact ABI, command semantics or complete transaction effects; the platform must verify that integration.

Authorized process claims must fit earned native liability, the initial `9960873688152935270`-wei six-hour cap, maximum `19921747376305870540`-wei cap and 24-claim rolling window. Existing cap-change delays and the 43200-second operations rotation delay remain. The bot's $25,000 to $50,000 dynamic USD budget is a separate control, not an onchain conversion of those fixed caps. Gas is manually funded; no automatic Treasury-to-Operations refill exists.

Every later cycle effect requires the configured authority, current quotes, exact recipients and amounts, reserves, durable intent and reconciliation. Uncertain effects remain uncertain until reconciled. Historical experiments, archive forks and prior submission results cannot establish current production admission, wallet authorization, deployment or finality. This package contains no signing or broadcast authority.
