# Assisted-launch revision verification plan

Bind evidence to the final repository commit and freshly generated source/artifact hashes for revision 0.1.2. Run the affected existing checks locally and obtain required CI results for that same commit. Older 0.1.1 results remain evidence for the old source only. Do not report all CI passed while any required check is pending or failing.

Reproduce the exact Solidity Standard JSON input, complete inline source closure and pinned dependencies using Solidity 0.8.26+commit.8a97fa7a, Cancun, optimizer enabled with 200 runs, viaIR true, metadata.bytecodeHash none and metadata.appendCBOR false. Verify ABI, creation bytecode, deployed-runtime template, link references and compiler-emitted immutable references for all three fully qualified production targets. Preserve the actual eighteen-field hook constructor tuple and mask `0x20cc`. Recompute hashes rather than reusing old runtime hashes or manually relabeling immutable IDs.

Verify cumulative 20/30/250 fee allocation within 300 bps and the current Programmable beneficiary. Cover cumulative rounding, native fee backing, authorized and unauthorized claims, both swap directions, exact-input and exact-output handling, partial-fill refusal, role restrictions, existing cap-change delays and operations rotation delay. Confirm processClaimMaxCount 24, initial six-hour cap `9960873688152935270` wei and maximum cap `19921747376305870540` wei.

Verify zero-ETH inventory initialization by launchAuthority after authorized allocation, custody binding and graph initialization. Check complete one-billion HKMN allocation, spacing 60, ticks 133500 and 161220, sqrtPriceX96 `250929875796514805540091219040452`, liquidity `421035394154913054639875`, and permanent position custody plus 115 atomic HKMN of rounding dust. No creator allocation or automatic Treasury-to-Operations refill is introduced. The fixed native caps remain independent of the bot's dynamic USD budget.

Retain the first-buyer presettlement fixture: actual buyer ETH settles before fee collection, using explicit native SETTLE, SWAP_EXACT_IN_SINGLE, then TAKE_ALL for HKMN. Retain the existing routing fixture with revised fee expectations. Label mock-router results, local PoolManager results and actual deployed-router tests separately. Programmable owns verification against the deployed UniversalRouter and its exact ABI; no platform adapter implementation is required in this preparation.

The platform resolves authoritative route namespaces, binding/runtime commitments, CREATE2 deployment bindings, permits and final transaction payloads, then verifies complete deployment, seeding and buyer simulation. Missing values remain unset; do not weaken a CLI gate to produce an apparently complete request. Record any actual packaging or preflight error verbatim with its request ID. Launch time, optional initial purchase, live deadlines and wallet nonces are not preparation prerequisites. No live request or credential use is part of this verification plan.
