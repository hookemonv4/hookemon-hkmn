# Native assisted-launch evidence

Revision 0.1.2 follows reviewed baseline `6f81c3fe86e541043847b140d4b6bc2f69ed846a` and supersedes submission `45ddd161-e5bb-4cb0-ad92-8f4eb044d82e` (0.1.1), after `e13314d0-af87-4635-ac40-d1cb5f4715ec` (0.1.0). Historical submission packages, their source hashes and their results remain unchanged.

The production compilation uses Solidity 0.8.26+commit.8a97fa7a, Cancun, optimizer enabled with 200 runs, viaIR true, metadata.bytecodeHash none and metadata.appendCBOR false. `build-info/launch.json` is the exact Solidity Standard JSON input, including the complete inline source closure and pinned dependencies. `artifacts/token.json`, `artifacts/custody.json` and `artifacts/hook.json` retain ABI, creation bytecode, runtime templates, link references and compiler-emitted immutable references. `review-target.json` inventories exact package digests. Compiler inputs and templates do not constitute a production API launch request or final constructor-bound runtime identity.

The targets are `src/HookemonHook.sol:HookemonHook`, `src/launch/HKMNToken.sol:HKMNToken` and `src/bindings/RobinhoodBindings.sol:PermanentPositionCustody`. Source path distinguishes the production launch token from another HKMNToken in the compiler closure. Constructor ABIs, the eighteen-field hook tuple and mask `0x20cc` remain intact. Generated hashes and immutable references must derive from the revised source and compiler outputs.

The current inventory configuration selects zero creator ETH, one billion HKMN at 18 decimals, spacing 60, ticks 133500 and 161220, sqrtPriceX96 `250929875796514805540091219040452`, liquidity `421035394154913054639875`, and permanent custody of the position and 115 atomic HKMN of rounding dust. Current fees are 20/30/250 bps within 300 bps. The claim configuration selects initial cap `9960873688152935270` wei, maximum cap `19921747376305870540` wei and processClaimMaxCount 24 per rolling six hours. These are selected preparation values, not evidence of deployed state.

Source and compiler artifacts are prepared for integration. The handoff must attach affected local test results and all required CI outcomes to the final commit. This document does not assert that every required CI check passed. Prior-source green checks, mock-router results, pinned archive-fork results and local synthetic deployment experiments each retain their actual scope; none proves the complete deployed UniversalRouter flow or current live admission. See `TEST_PLAN.md` for the required coverage.

`feasibility/native-funding/` and older release records contain dated experiments with funded seeds, earlier caps or full-range inventory. They remain historical evidence and do not select current wallet actions. Historical interface freezes and provisional build records provide only the scope they explicitly attest. Earlier owner discussion of a 10-bps platform share is superseded by the current revision; it is not admission evidence for this source.

Platform-controlled route namespaces, runtime/binding commitments, CREATE2 bindings, permits and final transaction payloads remain unresolved until supplied from authoritative records. Programmable performs deployment/seeding/buyer integration, exact authorization mapping and wallet transaction preparation. No new customer economic choice is required to prepare the package. Launch time, optional purchase, live deadlines and wallet nonces remain unset; later wallet review is required before signing. No deployment, funding transfer, live launch request or signature is evidenced here.
